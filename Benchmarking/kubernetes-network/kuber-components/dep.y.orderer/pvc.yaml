apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc.dep.y.orderer
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  volumeName: pv.dep.y.orderer
  storageClassName: standard
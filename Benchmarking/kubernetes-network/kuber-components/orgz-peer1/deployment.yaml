apiVersion: apps/v1
kind: Deployment
metadata:
  name: orgz-peer1-deployment
  labels:
    ledger: orgz-couch-peer1
    type-component: peer
    name-component: orgz-peer1
    msp: OrgzMSP
spec:
  replicas: 1
  selector:
    matchLabels:
      deployment: orgz-peer1
  template:
    metadata:
      labels:
        deployment: orgz-peer1
        service: orgz-peer1
        general: all-peers
        ledger: orgz-couch-peer1
        type-component: peer
        name-component: orgz-peer1
        msp: OrgzMSP
    spec:
      containers:
        - name: orgz-peer1-container
          image: hyperledger/fabric-peer
          env:
            - name: FABRIC_CFG_PATH
              value: "/tmp/hyperledger/orgz-peer1/config"
          volumeMounts:
            - mountPath: "/tmp/hyperledger/orgz-peer1/"
              name: orgz-peer1-volume
          lifecycle:
            postStart:
              exec:
                command:
                  - "/bin/sh"
                  - "-c"
                  - |
                    sleep 30s &&
                    apt update &&
                    apt install -y jq
                    chmod +777 /tmp/hyperledger/orgz-peer1/config/builder/bin/*
        - name: orgz-peer1-grpc-automation-container
          image: pedrosilvamnchunks/peer-grpc:latest
          env:
            - name: FABRIC_CFG_PATH
              value: "/tmp/hyperledger/orgz-peer1/config"
          volumeMounts:
            - mountPath: "/tmp/hyperledger/orgz-peer1"
              name: orgz-peer1-volume
      volumes:
        - name: orgz-peer1-volume
          persistentVolumeClaim:
            claimName: pvc-orgz-peer1

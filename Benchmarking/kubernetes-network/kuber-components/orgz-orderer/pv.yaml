apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-orgz-orderer
  labels:
    volume: orgz-orderer
spec:
  storageClassName: standard
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  local:
    path: /root/orgz-orderer
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - sa-srvaida77
